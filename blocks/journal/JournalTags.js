import React from 'react';

const JournalTags = ({ text }) => {
    return (
        <div className="tags">
            <h2 className="line">{text}</h2>
        </div>
    );
};

export default JournalTags;
